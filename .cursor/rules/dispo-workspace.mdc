---
description: Dateien nur im dispo dieses Workspaces bearbeiten (Kalender, API, App)
alwaysApply: true
---

# Dispo nur in diesem Workspace

Bei allen Änderungen an Kalender-, API- und App-Dateien der Dispo **ausschließlich** die Dateien im **dispo**-Ordner dieses Workspaces verwenden.

- **Pfad:** Immer den **relativen Pfad** `dispo/` vom **Workspace-Root** (Plattform `Kukla_Monteur_Plattform` auf OneDrive) nutzen (z. B. `dispo/modules/calendar.php`, `dispo/calendar_month.php`, `dispo/api/calendar.php`).
- **Nicht** absolute Pfade zu anderen Projektordnern verwenden – nur das **dispo**-Verzeichnis dieses Workspaces bearbeiten.
- **Hinweis:** Lokaler XAMPP/htdocs wird nicht mehr verwendet. Source of Truth ist der OneDrive-Workspace; Runtime ist der Ubuntu-Server (`/mnt/raid/kukla/dispo/current`).

Damit landen Änderungen dort, wo die Dispo-Anwendung für dieses Projekt geladen wird.
